import * as React from 'react';

export default class PropComponent extends React.Component {
  public render(): React.ReactNode {
    return (
      <div>
        this is prop component page
      </div>
    )
  }
}