import * as React from 'react';

export default class UiPageComponent extends React.Component {
  public render(): React.ReactNode {
    return (
      <div>
        this is Ui Page
      </div>
    )
  }
}